import React, { useState, useEffect} from 'react';
import GiveButton from "./Buttons/GiveButton";

export default function GiveThings() {

    return (
        <div className="giveThings">
            <GiveButton style={{marginBottom: "2vh"}}/>
        </div>
    )
}